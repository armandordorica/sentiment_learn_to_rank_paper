# News-sentiment fine-tuning stack (Hugging Face + PyTorch).
# Used by notebooks/liquidAI_prep.ipynb and the planned TRNA-substitute sentiment
# model (see docs/news_sentiment_finetuning_plan.md).
#
# Install into the project env:
#     pip install -r requirements-finetuning.txt
#
# Verified working in the `sentiment-ltr-paper` conda env (Python 3.11) alongside
# numpy 2.4 / pandas 2.2 / pyarrow 21 / scikit-learn 1.9 / fsspec 2026.4 with a
# clean `pip check`. Pinned-version reference from that solve is shown in comments.

torch>=2.4,<3            # verified: 2.12.1
transformers>=4.44,<6    # verified: 5.12.1
datasets>=3.0,<6         # verified: 5.0.0  (NOTE: v4+ dropped dataset loading scripts)
evaluate>=0.4,<1         # verified: 0.4.6
accelerate>=0.34,<2      # verified: 1.14.0
ipywidgets>=8,<9         # silences the tqdm/IProgress warning in Jupyter

# Heads-up: the canonical `financial_phrasebank` dataset is script-based and no
# longer loads on datasets v4/v5. Use a Parquet mirror instead, e.g.
# `atrost/financial_phrasebank` (the setup notebook already does this).
